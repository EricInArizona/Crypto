// Copyright Eric Chauvin, 2021.



#pragma once


/*

#include "BasicTypes.h"
#include "FileIO.h"



class ENumber
  {
  private:
  Int32 testForCopy = 123;
This would have to be digitArraySize times 32
for the number of bits.
Or do this 32 bits at a time.

On regular Integer.h numbers:
If Base is like 2^32 then bit 27 is 2^27 times
2^Base = 2^(32 + 27) so each bit has a particular
power.
Do the same kind of thing with each 32 bits of
these numbers.


  EPoint baseAr;

  public:

  };
*/
